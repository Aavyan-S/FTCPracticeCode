package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
public class FTCAprilTag extends LinearOpMode {
    // Do not use distance sensor with Webcam/Limelight // Farther away = less reliable. More angled = less reliable //Tag size, tag count // Recalibrate position only if reliable // Best to use both Odometry and April Tags together
    public void runOpMode() {
        //TFOD //OpenCV // Color blob locator
        waitForStart();
    }
}
